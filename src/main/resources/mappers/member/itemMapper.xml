<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.dbinc.com.web.repository.item.ItemMyBatisRepository">
    <select id="isExistItem" parameterType="String">
        SELECT EXISTS (SELECT 1
                       FROM ITEM
                       WHERE name = #{name})
    </select>

    <insert id="createItem" parameterType="ItemCreate" useGeneratedKeys="true" keyProperty="itemId">
        INSERT INTO ITEM(NAME,
                         PRICE,
                         STOCK_QUANTITY)
        VALUES (#{name},
                #{price},
                #{stockQuantity})
    </insert>

    <select id="findItemList" resultType="ItemQueryResponse">
        SELECT ITEM_ID,
               NAME,
               PRICE,
               STOCK_QUANTITY
        FROM ITEM
    </select>

    <update id="updateItem" parameterType="map">
        UPDATE ITEM
        SET NAME = #{itemReqeust.name},
            PRICE = #{itemReqeust.price},
            STOCK_QUANTITY = #{itemReqeust.stockQuantity}
        WHERE ITEM_ID = #{itemId}
    </update>

    <select id="selectItemById" parameterType="Long" resultType="ItemQueryResponse">
        SELECT ITEM_ID,
               NAME,
               PRICE,
               STOCK_QUANTITY
        FROM ITEM
        WHERE ITEM_ID = #{itemId}
    </select>
</mapper>